# @vercel/speed-insights declares @sveltejs/kit as an optional peer, which npm 11
# still tries to resolve. SvelteKit peers on vite@8 while vitest pulls vite@7, so
# npm reports a spurious ERESOLVE conflict for a package that is never installed.
legacy-peer-deps=true
